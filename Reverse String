#include <stdio.h>
#include <string.h>
int main()
{
    int len,i;
    char s1[]="Programming";
    len=strlen(s1);
    char s2[len];
    for(i=(len-1); i>=0; i--)
    {
        s2[len-1-i]=s1[i];
    }
    printf("%s",s2);
}
